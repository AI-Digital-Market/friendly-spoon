// Placeholder routes - will be created based on specific requirements

import express from 'express';
const router = express.Router();

// Blog routes placeholder
router.get('/', (req, res) => {
  res.json({ message: 'Blog API - Coming Soon' });
});

export default router;
