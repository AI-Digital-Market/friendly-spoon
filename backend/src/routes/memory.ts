// Placeholder routes - will be created based on specific requirements

import express from 'express';
const router = express.Router();

// Memory routes placeholder
router.get('/', (req, res) => {
  res.json({ message: 'Memory API - Coming Soon' });
});

export default router;
